# ============================================================
# PIK — Environment Variables
#
# Copy to .env for local development.
# In Railway, set these in Dashboard → Variables.
# ============================================================

# ── Database ─────────────────────────────────────────────────
# Railway auto-injects this when you add a Postgres plugin.
# For local development, point to a local Postgres instance.
DATABASE_URL=postgresql://postgres:password@localhost:5432/pik

# ── Server ───────────────────────────────────────────────────
PORT=8080
NODE_ENV=development

# ── WebAuthn / Passkeys ──────────────────────────────────────
# RP = Relying Party. These MUST match your deployment domain.
#
# Local development:
WEBAUTHN_RP_NAME=PIK - Persistent Identity Kernel
WEBAUTHN_RP_ID=localhost
WEBAUTHN_ORIGIN=http://localhost:8080
#
# Railway production (update to your actual domain):
# WEBAUTHN_RP_ID=your-app.railway.app
# WEBAUTHN_ORIGIN=https://your-app.railway.app
